#include <dslib/primitives.h>

int main(void) {
    Point line_from = { 0, 0 };
    Point line_to = { 1, 1 };

    const int N = 2;
    Point pts[N] = {
        { 0, 1 },
        { 1, 0 }
    };

    for (int i = 0; i < N; ++i) {
        
    }
}